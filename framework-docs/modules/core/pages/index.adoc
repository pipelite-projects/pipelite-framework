= Core
:description: Documentation for the Pipelite core module

The `pipelite-core` module contains the framework's execution engine.
This section describes the fundamental concepts required to understand how messages are processed along a pipeline.

[cols="1,3"]
|===
|Page |Content

|xref:exchange.adoc[Exchange]
|Structure of the `Exchange`, access to input/output, headers, and properties.

|xref:messages.adoc[Message and Headers]
|The `Message`, the payload, and the `Headers` API.

|xref:flows.adoc[Flow and PipeliteContext]
|Lifecycle, `FlowRegistry`, `PipeliteContext`, `FlowDefinition`.

|xref:flow-declaration.adoc[Declaring Flows]
|Declarative flow registration via `@FlowConfiguration` and `@DefineFlow`: plain-Java and Spring Boot usage.

|xref:processors.adoc[Processors and Transformers]
|`Processor`, `PayloadTransformer`, WireTap, and all node types.

|xref:error-handling.adoc[Error Handling]
|Error channel, retry channel, `FlowExecutionDump`.
|===
